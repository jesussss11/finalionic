import { FilterasocPipe } from './filterasoc.pipe';

describe('FilterasocPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterasocPipe();
    expect(pipe).toBeTruthy();
  });
});
